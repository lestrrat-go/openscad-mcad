package mcad

/*
func HelixCurve(ctx context.Context, w io.Writer) error {
	dsl.Function("helix_curve").
		Parameters(pitch, radius, z).
		Value(
			dsl.List(
				dsl.Mul(radius, dsl.Cos(
					dsl.
				))
			)
		)
}
*/
